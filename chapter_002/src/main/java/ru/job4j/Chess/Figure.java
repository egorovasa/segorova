/*package ru.job4j.Chess;*/
/**
 * Абстрактный класс Figure.
 */
/*public abstract class Figure {

     final Figure[] figures = new Figure[32];
     int index = 0;
    /**
     * position возвращает текущую клетку, которую мы занимаем.

    Cell position;
    /**
     * Метод, который проверяет, что данная фигура может так двигаться.
     *
     * @param source Источник, откуда мы идём.
     *
     * @param dest Destination, куда мы хотим пойти.
     *
     * @return
     *
     */
   /* Cell[] way(Cell source, Cell dest);*/
   /* /**
     * Метод copy "перезаписывает" фигуру.
     *
     * @param dest
     *
     * @return
     *
     */
    /*Figure copy(Cell dest);

    public boolean move(Cell source, Cell dest) {
        boolean rst = false;
        int index = findBy(sourse);
        if (index != -1) {
            Cell[] steps = this.figures[index].way(sourse, dest);
            if (steps.length > 0 && steps[steps.length - 1].equals(dest)) {
                rst = true;
                this.figures[index] = this.figures[index].copy(dest);
            }
        }
        return rst;
    }
   private int findBy(Cell cell) {
        int rst = -1;
        for (int index = 0; index != this.figures.length; index++) {
            if (this.figures[index] != null && this.figures[index].position.equals(cell)) {
                rst = index;
                break;
            }
        }
        return rst;
   }
}*/

